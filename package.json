{
  "name": "pantheon",
  "version": "0.0.1",
  "private": true,
  "description": "Coordination layer for AI personas — persona identity, memory, real-time chat, and session lifecycle in one daemon. Ships as both a vanilla MCP server and a Claude Code plugin.",
  "type": "module",
  "module": "src/index.ts",
  "bin": {
    "pantheon": "bin/pantheon.ts",
    "pantheon-fetch": "bin/pantheon-fetch.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bun tsc --noEmit",
    "console": "bun bin/pantheon.ts console"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
