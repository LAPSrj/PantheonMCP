Sole registration for `{{cwd}}` is `{{username}}`.

If that's you, call `claim({ username: "{{username}}" })` to adopt
the identity (and load its memory). If it's not, register a fresh
handle for yourself.
