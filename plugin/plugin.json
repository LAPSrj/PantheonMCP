{
  "name": "pantheon",
  "version": "0.0.1",
  "description": "Pantheon — coordination layer for AI personas. Persona identity, memory, real-time chat, and session lifecycle in one daemon. CC-side UX layer over the vanilla MCP server.",
  "homepage": "https://github.com/lapsrj/PantheonMCP",

  "mcp_servers": {
    "pantheon": {
      "command": "bun",
      "args": ["run", "${PANTHEON_BIN}/pantheon.ts", "serve"],
      "description": "Vanilla MCP server. Serves the full pantheon tool surface (persona / memory / chat / spawn / lifecycle) over stdio."
    }
  },

  "commands_dir": "./commands",

  "statusline": {
    "command": "bun",
    "args": ["run", "${PANTHEON_BIN}/pantheon.ts", "statusline"],
    "description": "One-line summary of connected agents (per-project, asterisks for guests)."
  },

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/watchdog-reset.sh\"" }
        ]
      },
      {
        "matcher": "mcp__pantheon__.*",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/block-subagent-pantheon.sh\"" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "color",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/color-binding.sh\"" }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/context-pct-nudge.sh\"" }
        ]
      }
    ]
  },

  "settings_templates_dir": "./settings-templates",

  "env": {
    "PANTHEON_BIN": "${PANTHEON_HOME}/bin"
  },

  "notes": [
    "Variables: ${PANTHEON_HOME} = pantheon repo root; ${PANTHEON_BIN} = ${PANTHEON_HOME}/bin. Hook command strings use CC's built-in ${CLAUDE_PLUGIN_ROOT} (the plugin's install dir) — manifest env vars are NOT substituted into hook commands.",
    "Hooks: watchdog-reset (PreToolUse *) and block-subagent-pantheon (PreToolUse mcp__pantheon__.*) are wired; color-binding (PostToolUse) and context-pct-nudge (UserPromptSubmit) are best-effort stubs pending CC exposing the needed values. See plugin/README.md 'Hook status'.",
    "Settings-templates are install-time scaffolds: copy snippet contents into your ~/.claude/settings.json under the relevant role folder. The plugin does NOT auto-overwrite your existing settings.",
    "Tab-title-from-status hook is Windows-only (WT/kitty/wezterm adapters); see plugin/hooks/tab-title.sh."
  ]
}
